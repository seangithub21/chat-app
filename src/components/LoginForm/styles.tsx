const getStyles = () => ({
  container: {
    backgroundColor: "#fff",
    borderRadius: "1rem",
    boxShadow: "rgba(149, 157, 165, 0.2) 0px 8px 24px",
    maxWidth: "35rem",
    padding: "3rem",
  },
  heading: {
    marginBottom: "2rem",
    textAlign: "center",
  },
});

export default getStyles;
