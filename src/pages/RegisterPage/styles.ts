const getStyles = () => ({
  container: {
    alignItems: "center",
    display: "flex",
    flexDirection: "column" as const,
    justifyContent: "center",
    minHeight: "100%",
    padding: "3rem",
  },
});

export default getStyles;
