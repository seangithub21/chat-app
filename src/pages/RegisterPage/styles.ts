const getStyles = () => ({
  container: {
    alignItems: "center",
    display: "flex",
    flexDirection: "column" as const,
    minHeight: "100vh",
    justifyContent: "center",
  },
});

export default getStyles;
